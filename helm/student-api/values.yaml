namespace: student-api

image:
  repository: student-api
  tag: "1.0.1"
  pullPolicy: Never

replicas: 2

service:
  type: NodePort
  port: 5000
  nodePort: 30000

nodeSelector:
  type: application

db:
  name: student_db
  host: postgres
  port: "5432"

flask:
  app: run.py
  debug: "false"
